#include "TimeSystem.h"

// Defining the constructor for the TimeSystem struct
TimeSystem::TimeSystem(int _year, int _month, int _day, int _hour, int _minute, double _second) {
    year = _year;
    month = _month;
    day = _day;
    hour = _hour;
    minute = _minute;
    second = _second;
}